# Copy this file to .env and fill in real values.
# Same Supabase project as the HireStack website - the anon key is safe
# to bundle into the app, same as it is in the web app's client bundle.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
